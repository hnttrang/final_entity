export enum eventStatus{
    ready = 'ready',
    sale = 'sale',
    staring = 'staring',
    ended = 'ended',
    cancelled = 'cancelled'
}

export enum Gender{
    male = 'male',
    female = 'female',
    other = 'other'
}

export enum ticketStatus{
   ready = 'ready',
   sold = 'sold' 
}

export enum orderStatus{
    progress = 'progress',
    done = 'done',
    cancelled = 'cancelled'
}